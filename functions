//This is an introduction to functions
#include <stdio.h>
find_max(int,int);
main()
{
    int x=10,y=7:
    printf("Maximum value is: %d\n", find_max(x,y));
}
int find_max(int x, int y)
{
    int max;
    if(x>y)
    max = x;
    else
    max =y;
    return max;
}
