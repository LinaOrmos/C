//this codes looks for the frequency in an array 
#include <stdio.h>

int find_freq(int a[], int n, int x) {
    
}
int main()
{
    int a[10],n,i, x;
    printf("Enter size of array: \n");
    scanf("%d", &n);
    printf("Enter %d elements \n",n);
    for(i=0;i<n;i++)
    scanf("%d",&a[i])
    printf("Enter element to be searched: \n");
    printf("Frecuency of %d is %d", )
    
}
